<%= form_with model: current_user.active_relationships.build do |f| %>
    <%# <%= user.id %>
    <div> <%= hidden_field_tag :followee_id, user.id %>
    <%= f.submit "Follow", class: "btn btn-primary" %>
    </div>
<% end %>